[["Period","New Amount", "New Count", "New Average","Canceled Amount", "Canceled Count", "Canceled Average","Declined Amount", "Declined Count", "Declined Average","Accepted Amount", "Accepted Count", "Accepted Average"],
{% for proposal in proposals %}["{{ proposal.0 }}",{{ proposal.1 }},{{ proposal.2 }},{{ proposal.3 }},{{ proposal.4 }},{{ proposal.5 }},{{ proposal.6 }},{{ proposal.7 }},{{ proposal.8 }},{{ proposal.9 }},{{ proposal.10 }},{{ proposal.11 }},{{ proposal.12 }}]{% if not forloop.last %},{% endif %}{% endfor %}
]

